databases:
   foo:
      driverClass: org.h2.Driver
      url: jdbc:h2:mem:foo
      user: "sa"
      password: ""

http:
   configured:
      meteo:
         url: http://localhost:8089/meteo/{{city}}
         headers:
            accept: text/{{format}}

     
